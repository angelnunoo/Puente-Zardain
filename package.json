{
  "name": "puente-zardain",
  "version": "0.1.0",
  "description": "Plataforma web de pedidos y fidelización para restaurante Puente de Zardain",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "bootstrap": "npm install",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm test --workspaces --if-present"
  },
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ]
}